<nav class="nav-container">
  <div class="nav-content">
    <a href="/" class="nav-logo">
      <span class="blink">></span> The Guide
    </a>
    <div class="nav-links">
      <a href="/babel-fish" class="nav-link">Babel Fish</a>
      <a href="/deep-thought" class="nav-link">Deep Thought</a>
      <a href="/infinite-improbability" class="nav-link">Infinite Improbability</a>
      <a href="/towels" class="nav-link">Towels</a>
      <a href="/universe" class="nav-link">Life, Universe & Everything</a>
      <a href="/search" class="nav-link search-link">🔍 Search</a>
    </div>
  </div>
</nav>

<style>
  .nav-container {
    position: sticky;
    top: 0;
    left: 0;
    right: 0;
    background: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--guide-green);
    z-index: 100;
  }

  .nav-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 1rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .nav-logo {
    color: var(--guide-green);
    text-decoration: none;
    font-size: 1.2rem;
    font-weight: bold;
    display: flex;
    align-items: center;
    gap: 0.5rem;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

  .blink {
    animation: blink 1s infinite;
  }

  .nav-links {
    display: flex;
    gap: 1.5rem;
  }

  .nav-link {
    color: var(--guide-green);
    text-decoration: none;
    opacity: 0.8;
    transition: all 0.2s;
  }

  .nav-link:hover {
    opacity: 1;
    text-shadow: 0 0 10px var(--guide-green);
  }

  .search-link {
    margin-left: 1rem;
    padding: 0.2rem 0.8rem;
    border: 1px solid var(--guide-green);
    border-radius: 4px;
  }

  .search-link:hover {
    background: rgba(37, 248, 142, 0.1);
  }

  @media (max-width: 768px) {
    .nav-content {
      flex-direction: column;
      gap: 1rem;
      padding: 1rem;
    }

    .nav-links {
      flex-wrap: wrap;
      justify-content: center;
      gap: 1rem;
    }
  }
</style>