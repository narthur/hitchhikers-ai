<nav class="nav-container">
  <div class="nav-content">
    <a href="/" class="nav-logo">
      <span class="blink">></span> The Guide
    </a>
    <div class="nav-links">
      <button class="nav-link random-link">🎲 Random Article</button>
      <a href="/search" class="nav-link search-link">🔍 Search</a>
    </div>
  </div>
</nav>

<style>
  .nav-container {
    position: sticky;
    top: 0;
    left: 0;
    right: 0;
    background: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(10px);
    border-bottom: 1px solid var(--guide-green);
    z-index: 100;
  }

  .nav-content {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0.3rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 2rem;
  }

  .nav-logo {
    color: var(--guide-green);
    text-decoration: none;
    font-size: 1.2rem;
    font-weight: bold;
    display: flex;
    align-items: center;
    gap: 0.5rem;
    flex-shrink: 0;
  }

  @keyframes blink {
    0%, 100% { opacity: 1; }
    50% { opacity: 0; }
  }

  .blink {
    animation: blink 1s infinite;
  }

  .nav-links {
    display: flex;
    gap: 1.5rem;
    align-items: center;
    justify-content: flex-end;
    flex-wrap: wrap;
    flex: 1;
    padding: 0.5rem 0;
  }

  .nav-link {
    color: var(--guide-green);
    text-decoration: none;
    opacity: 0.8;
    transition: all 0.2s;
    padding: 0.3rem 0.8rem;
    line-height: 1;
    border: 1px solid var(--guide-green);
    border-radius: 4px;
    background: transparent;
    font-family: 'Share Tech Mono', monospace;
    font-size: 1rem;
    cursor: pointer;
  }

  button.nav-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }

  .nav-link:hover {
    opacity: 1;
    text-shadow: 0 0 10px var(--guide-green);
    background: rgba(37, 248, 142, 0.1);
  }

  .nav-link:active {
    background: rgba(37, 248, 142, 0.2);
    transform: translateY(1px);
  }

  .nav-link:focus {
    outline: none;
    box-shadow: 0 0 0 2px rgba(37, 248, 142, 0.3);
  }

  @media (max-width: 768px) {
    .nav-content {
      flex-direction: column;
      gap: 1rem;
      padding: 1rem;
    }

    .nav-links {
      justify-content: center;
      gap: 1rem 1.5rem;
      padding: 0;
    }
  }
</style>

<script>
  const GUIDE_PATHS = [
    '/babel-fish',
    '/deep-thought',
    '/infinite-improbability',
    '/towels',
    '/universe'
  ];

  const getRandomPath = () => {
    const randomIndex = Math.floor(Math.random() * GUIDE_PATHS.length);
    return GUIDE_PATHS[randomIndex];
  };

  document.querySelector('.random-link')?.addEventListener('click', () => {
    window.location.href = getRandomPath();
  });
</script>