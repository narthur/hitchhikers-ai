<div id="stars"></div>
<div id="stars2"></div>
<div id="stars3"></div>

<div id="container">
	<main>
		<section id="hero">
			<h1>DON'T PANIC</h1>
			<p class="subtitle">Welcome to the Hitchhiker's Guide to AI</p>
			
			<section id="links">
				<a class="button" href="https://docs.astro.build">Read the Guide</a>
				<a class="button" href="https://astro.build/chat">Join the Discussion</a>
			</section>
		</section>
	</main>

	<div id="news" class="box">
		<h2>About the Guide</h2>
		<p>
			The Hitchhiker's Guide to AI has this to say about artificial intelligence: 
			"Mostly Harmless."
		</p>
		<p class="note">
			Note: This guide is slightly cheaper than the Encyclopedia Galactica.
		</p>
	</div>

	<div id="towel-notice" class="box">
		You remembered to bring your towel, right?
	</div>
</div>

<style>
	#container {
		height: 100%;
		padding: 2rem;
	}

	main {
		height: 100%;
		display: flex;
		justify-content: center;
		max-width: 800px;
		margin: 0 auto;
	}

	#hero {
		display: flex;
		align-items: center;
		flex-direction: column;
		justify-content: center;
		text-align: center;
		padding: 16px;
	}

	h1 {
		font-size: 4rem;
		margin: 0;
		letter-spacing: 4px;
		text-shadow: 0 0 20px var(--guide-green);
		animation: pulse 2s infinite;
	}

	@keyframes pulse {
		0% { text-shadow: 0 0 20px var(--guide-green); }
		50% { text-shadow: 0 0 40px var(--guide-green); }
		100% { text-shadow: 0 0 20px var(--guide-green); }
	}

	.subtitle {
		font-size: 1.5rem;
		margin: 1rem 0 2rem;
		opacity: 0.8;
	}

	#links {
		display: flex;
		gap: 16px;
	}

	.button {
		display: flex;
		align-items: center;
		padding: 12px 24px;
		color: #000;
		background: var(--guide-green);
		text-decoration: none;
		border-radius: 4px;
		font-weight: bold;
		transition: all 0.2s;
	}

	.button:hover {
		background: #fff;
		box-shadow: 0 0 20px var(--guide-green);
		transform: scale(1.05);
	}

	.box {
		background: rgba(37, 248, 142, 0.1);
		border: 1px solid var(--guide-green);
		border-radius: 8px;
		padding: 1.5rem;
		backdrop-filter: blur(5px);
	}

	#news {
		position: fixed;
		bottom: 2rem;
		right: 2rem;
		max-width: 400px;
	}

	#towel-notice {
		position: fixed;
		top: 2rem;
		left: 2rem;
		font-size: 0.9rem;
		opacity: 0.8;
	}

	#news h2 {
		margin: 0 0 1rem;
		font-size: 1.2rem;
	}

	#news p {
		margin: 0;
		line-height: 1.4;
	}

	.note {
		margin-top: 1rem !important;
		font-size: 0.9rem;
		opacity: 0.8;
	}

	@media screen and (max-width: 768px) {
		h1 {
			font-size: 3rem;
		}

		#links {
			flex-direction: column;
		}

		#news {
			position: static;
			margin-top: 2rem;
			max-width: 100%;
		}

		#towel-notice {
			position: static;
			margin-bottom: 2rem;
		}
	}
</style>
