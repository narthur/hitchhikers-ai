<div id="stars"></div>
<div id="stars2"></div>
<div id="stars3"></div>

<div id="container">
	<main>
		<section id="hero">
			<h1>DON'T PANIC</h1>
			<p class="subtitle">The Hitchhiker's Guide has this to say about artificial intelligence:</p>
			<div class="guide-entry">
				<p class="quote">"Space," it says, "is big. Really big. You just won't believe how vastly, hugely, mind-bogglingly big it is." But compared to the exponential growth of artificial intelligence, space seems rather cozy and well-defined.</p>
			</div>
			
			<section id="links">
				<a href="/search" class="button search-button">
					<span class="blink">&gt;</span>
					<span>Search the Guide</span>
				</a>
				<a href="/deep-thought" class="button">Ask Deep Thought</a>
			</section>
		</section>
	</main>

	<div id="news" class="box">
		<h2>Latest Entry</h2>
		<p>
			"The Encyclopedia Galactica defines AI as 'silicon-based intelligence that mostly doesn't try to take over the universe.' The Hitchhiker's Guide has this to say: 'Mostly Harmless.'"
		</p>
		<p class="note">
			Note: The Guide is slightly cheaper than the Encyclopedia Galactica and has the words "DON'T PANIC" inscribed in large, friendly letters on its cover.
		</p>
	</div>

	<div id="towel-notice" class="box">
		<span class="blink">&gt;</span> Important Notice: Always bring a towel, and never forget where your neural network is.
	</div>
</div>

<style>
	@keyframes blink {
		0%, 100% { opacity: 1; }
		50% { opacity: 0; }
	}

	.blink {
		animation: blink 1s infinite;
		margin-right: 0.5rem;
		display: inline-block;
	}

	#container {
		height: 100%;
		padding: 2rem;
	}

	main {
		height: 100%;
		display: flex;
		justify-content: center;
		max-width: 800px;
		margin: 0 auto;
	}

	#hero {
		display: flex;
		align-items: center;
		flex-direction: column;
		justify-content: center;
		text-align: center;
		padding: 16px;
		width: 100%;
	}

	h1 {
		font-size: 5rem;
		margin: 0;
		letter-spacing: 4px;
		text-shadow: 0 0 20px var(--guide-green);
		animation: pulse 2s infinite;
	}

	@keyframes pulse {
		0% { text-shadow: 0 0 20px var(--guide-green); }
		50% { text-shadow: 0 0 40px var(--guide-green); }
		100% { text-shadow: 0 0 20px var(--guide-green); }
	}

	.subtitle {
		font-size: 1.5rem;
		margin: 1rem 0;
		opacity: 0.8;
	}

	.guide-entry {
		background: rgba(37, 248, 142, 0.1);
		border: 1px solid var(--guide-green);
		border-radius: 8px;
		padding: 2rem;
		margin: 1rem 0 2rem;
		max-width: 800px;
		backdrop-filter: blur(5px);
	}

	.quote {
		font-size: 1.2rem;
		line-height: 1.6;
		margin: 0;
		font-style: italic;
		text-align: left;
	}

	#links {
		display: flex;
		gap: 1rem;
		flex-wrap: wrap;
		justify-content: center;
		max-width: 600px;
	}

	.button {
		display: flex;
		align-items: center;
		padding: 12px 24px;
		color: #000;
		background: var(--guide-green);
		text-decoration: none;
		border-radius: 4px;
		font-weight: bold;
		transition: all 0.2s;
		min-width: 180px;
		justify-content: center;
	}

	.button:hover {
		background: #fff;
		box-shadow: 0 0 20px var(--guide-green);
		transform: scale(1.05);
	}

	.search-button {
		background: transparent;
		border: 2px solid var(--guide-green);
		color: var(--guide-green);
	}

	.search-button:hover {
		background: rgba(37, 248, 142, 0.1);
		color: var(--guide-green);
	}

	.box {
		background: rgba(37, 248, 142, 0.1);
		border: 1px solid var(--guide-green);
		border-radius: 8px;
		padding: 1.5rem;
		backdrop-filter: blur(5px);
		max-width: 400px;
	}

	#news {
		position: fixed;
		bottom: 2rem;
		right: 2rem;
	}

	#towel-notice {
		position: fixed;
		top: 2rem;
		left: 2rem;
		font-size: 0.9rem;
		opacity: 0.8;
		display: flex;
		align-items: center;
		gap: 0.5rem;
	}

	#news h2 {
		margin: 0 0 1rem;
		font-size: 1.2rem;
	}

	#news p {
		margin: 0;
		line-height: 1.4;
	}

	.note {
		margin-top: 1rem !important;
		font-size: 0.9rem;
		opacity: 0.8;
		font-style: italic;
	}

	@media screen and (max-width: 768px) {
		h1 {
			font-size: 3rem;
		}

		.guide-entry {
			margin: 1rem;
			padding: 1.5rem;
		}

		.quote {
			font-size: 1.1rem;
		}

		#links {
			flex-direction: column;
			width: 100%;
			padding: 0 1rem;
		}

		.button {
			width: 100%;
		}

		#news {
			position: static;
			margin: 2rem auto;
			max-width: calc(100% - 2rem);
		}

		#towel-notice {
			position: static;
			margin: 1rem auto;
			max-width: calc(100% - 2rem);
		}
	}
</style>
